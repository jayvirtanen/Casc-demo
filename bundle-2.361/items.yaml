removeStrategy:
  items: 'NONE'
  rbac: 'SYNC'

items:
  - kind: freeStyle
    name: freestyle-example
    displayName: Freestyle Job example
    label: agent
    builders:
      - shell:
          command: |-
            #!/usr/bin/env bash
            echo "Hello world"
